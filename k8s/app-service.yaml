apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: article-platform
  labels:
    app: article-platform-app
spec:
  type: NodePort
  selector:
    app: article-platform-app
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    nodePort: 30080