<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DevOps Articles - Explore</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/app.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark app-navbar sticky-top">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="/articles">
        <i class="fas fa-feather-alt me-2"></i>
        DevOps Articles
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#primaryNav" aria-controls="primaryNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="primaryNav">
        <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/articles">
              <i class="fas fa-compass me-1"></i>
              Explore
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/articles/mine">
              <i class="fas fa-user-edit me-1"></i>
              My Articles
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/articles/create">
              <i class="fas fa-plus me-1"></i>
              Create
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/health">
              <i class="fas fa-heartbeat me-1"></i>
              Health
            </a>
          </li>
        </ul>
        <% if (currentUser) { %>
          <div class="d-flex align-items-center gap-3 ms-lg-4 mt-3 mt-lg-0">
            <% if (currentUser.isAdmin) { %>
              <a href="/admin" class="btn btn-warning btn-sm rounded-pill me-2">
                <i class="fas fa-shield-alt me-1"></i>
                Admin Panel
              </a>
            <% } %>
            <span class="text-white-50">
              <i class="fas fa-user-circle me-1"></i>
              <%= currentUser.email %>
            </span>
            <form method="POST" action="/auth/logout" class="m-0">
              <button type="submit" class="btn btn-outline-light btn-sm rounded-pill">
                <i class="fas fa-sign-out-alt me-1"></i>
                Sign out
              </button>
            </form>
          </div>
        <% } else { %>
          <div class="d-flex align-items-center gap-2 ms-lg-4 mt-3 mt-lg-0">
            <a href="/auth/login" class="btn btn-outline-light btn-sm rounded-pill">
              <i class="fas fa-sign-in-alt me-1"></i>
              Login
            </a>
            <a href="/auth/signup" class="btn btn-light btn-sm rounded-pill">
              <i class="fas fa-user-plus me-1"></i>
              Sign Up
            </a>
            <a href="/auth/admin/login" class="btn btn-warning btn-sm rounded-pill">
              <i class="fas fa-shield-alt me-1"></i>
              Admin
            </a>
          </div>
        <% } %>
      </div>
    </div>
  </nav>

  <main class="app-shell">
    <div class="container">
      <%- include('partials/flash') %>

      <section class="hero-banner mb-5">
        <div class="row align-items-center g-4">
          <div class="col-lg-8">
            <h1 class="fw-bold mb-3">
              Discover DevOps perspectives crafted by the community
            </h1>
            <p class="text-muted mb-0">
              Curated articles, personal case studies, and actionable insights. Search, save, and share knowledge with your peers.
            </p>
          </div>
          <div class="col-lg-4 text-lg-end">
            <a href="/articles/create" class="btn btn-primary-pill btn-pill">
              <i class="fas fa-plus-circle"></i>
              New Article
            </a>
          </div>
        </div>
      </section>

      <section class="mb-5">
        <form method="GET" action="/articles" class="search-wrapper">
          <div class="search-input">
            <i class="fas fa-search"></i>
            <input
              type="text"
              name="search"
              value="<%= searchTerm || '' %>"
              class="form-control"
              placeholder="Search articles by title or keywords..."
              aria-label="Search articles"
            >
          </div>
          <div class="search-buttons">
            <button type="submit" class="btn btn-primary-pill btn-pill btn-sm">
              <i class="fas fa-search"></i>
              Search
            </button>
            <a href="/articles" class="btn btn-outline-pill btn-sm">
              <i class="fas fa-times"></i>
              Clear
            </a>
          </div>
        </form>
      </section>

      <% if (articles.length === 0) { %>
        <section class="app-card p-5 text-center">
          <i class="fas fa-newspaper fa-3x text-primary mb-3"></i>
          <h2 class="fw-bold mb-3">
            <% if (searchTerm) { %>
              No results for "<%= searchTerm %>"
            <% } else { %>
              No articles yet
            <% } %>
          </h2>
          <p class="lead text-muted mb-4">
            <% if (searchTerm) { %>
              Try searching with different keywords or explore your private drafts.
            <% } else { %>
              Be the first to share a DevOps insight with the community.
            <% } %>
          </p>
          <a href="/articles/create" class="btn btn-secondary-pill btn-pill">
            <i class="fas fa-pen-nib"></i>
            Create an article
          </a>
        </section>
      <% } else { %>
        <section class="article-grid">
          <% articles.forEach(article => { %>
            <article class="article-card h-100">
              <% const heroImage = (article.media || []).find(item => item.type === 'image') %>
              <% if (heroImage) { %>
                <img src="<%= heroImage.url || ('/' + heroImage.storagePath) %>" class="w-100" style="height: 180px; object-fit: cover;" alt="<%= article.title || 'Article image' %>">
              <% } %>
              <div class="article-header">
                <h2 class="article-title d-flex align-items-center gap-2">
                  <i class="fas fa-file-lines text-primary"></i>
                  <%= article.title || 'Untitled Article' %>
                </h2>
                <div class="small text-muted">
                  <i class="fas fa-calendar me-1"></i>
                  <%= new Date(article.createdAt).toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                  }) %>
                </div>
              </div>
              <div class="article-body">
                <% const previewText = article.body ? article.body.replace(/<[^>]+>/g, '') : '' %>
                <p>
                  <%= previewText ? (previewText.length > 150 ? previewText.substring(0, 150) + '…' : previewText) : 'This article is waiting for its content.' %>
                </p>

                <% if (article.owner) { %>
                  <div class="d-flex align-items-center text-secondary small mt-3">
                    <i class="fas fa-user-circle me-2"></i>
                    <span>By <%= article.owner.email %></span>
                  </div>
                <% } %>

                <% const docs = (article.media || []).filter(item => item.type === 'document') %>
                <% const videos = (article.media || []).filter(item => item.type === 'video') %>
                <% if (docs.length || videos.length) { %>
                  <div class="d-flex flex-wrap gap-2 mt-3">
                    <% if (docs.length) { %>
                      <span class="badge-soft bg-primary-subtle text-primary">
                        <i class="fas fa-file me-1"></i>
                        <%= docs.length %> doc<%= docs.length > 1 ? 's' : '' %>
                      </span>
                    <% } %>
                    <% if (videos.length) { %>
                      <span class="badge-soft bg-secondary-subtle text-secondary">
                        <i class="fas fa-video me-1"></i>
                        <%= videos.length %> video<%= videos.length > 1 ? 's' : '' %>
                      </span>
                    <% } %>
                  </div>
                <% } %>

                <div class="mt-3">
                  <% if (article.approved) { %>
                    <span class="badge-soft bg-success-subtle text-success">
                      <i class="fas fa-circle-check me-1"></i>
                      Published
                    </span>
                  <% } else { %>
                    <span class="badge-soft bg-warning-subtle text-warning">
                      <i class="fas fa-clock me-1"></i>
                      Pending Review
                    </span>
                  <% } %>
                </div>
              </div>
              <div class="article-actions">
                <a href="/articles/<%= article.id %>" class="btn btn-primary-pill btn-pill btn-sm">
                  <i class="fas fa-eye"></i>
                  Read
                </a>
                <% if (currentUserId && article.userId === currentUserId) { %>
                  <a href="/articles/update/<%= article.id %>" class="btn btn-warning-pill btn-pill btn-sm">
                    <i class="fas fa-edit"></i>
                    Edit
                  </a>
                  <form method="POST" action="/articles/<%= article.id %>?_method=DELETE" class="d-inline">
                    <button type="submit" class="btn btn-danger-pill btn-pill btn-sm"
                      onclick="return confirm('Delete this article? This action cannot be undone.')">
                      <i class="fas fa-trash"></i>
                      Delete
                    </button>
                  </form>
                <% } %>
              </div>
            </article>
          <% }) %>
        </section>
      <% } %>
    </div>
  </main>

  <a href="/articles/create" class="fab-create" title="Create new article">
    <i class="fas fa-plus"></i>
  </a>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
