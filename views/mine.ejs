<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DevOps Articles - My Library</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/app.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark app-navbar sticky-top">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="/articles">
        <i class="fas fa-feather-alt me-2"></i>
        DevOps Articles
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#primaryNav" aria-controls="primaryNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="primaryNav">
        <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-2">
          <li class="nav-item">
            <a class="nav-link" href="/articles">
              <i class="fas fa-compass me-1"></i>
              Explore
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/articles/mine">
              <i class="fas fa-bookmark me-1"></i>
              My Articles
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/articles/create">
              <i class="fas fa-plus me-1"></i>
              Create
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/health">
              <i class="fas fa-heartbeat me-1"></i>
              Health
            </a>
          </li>
        </ul>
        <% if (currentUser) { %>
          <div class="d-flex align-items-center gap-3 ms-lg-4 mt-3 mt-lg-0">
            <span class="text-white-50">
              <i class="fas fa-user-circle me-1"></i>
              <%= currentUser.email %>
            </span>
            <form method="POST" action="/auth/logout" class="m-0">
              <button type="submit" class="btn btn-outline-light btn-sm rounded-pill">
                <i class="fas fa-sign-out-alt me-1"></i>
                Sign out
              </button>
            </form>
          </div>
        <% } %>
      </div>
    </div>
  </nav>

  <main class="app-shell">
    <div class="container">
      <%- include('partials/flash') %>

      <section class="hero-banner mb-5">
        <div class="row align-items-center g-4">
          <div class="col-lg-8">
            <h1 class="fw-bold mb-3">
              Your personal DevOps knowledge base
            </h1>
            <p class="text-muted mb-0">
              Manage drafts, polish private notes, and publish the stories you’re ready to share with the world.
            </p>
          </div>
          <div class="col-lg-4 text-lg-end">
            <a href="/articles/create" class="btn btn-primary-pill btn-pill">
              <i class="fas fa-plus-circle"></i>
              Start writing
            </a>
          </div>
        </div>
      </section>

      <section class="mb-5">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-3">
          <h2 class="h4 fw-bold mb-0 d-flex align-items-center gap-2">
            <i class="fas fa-lock text-secondary"></i>
            Private Workspace
            <span class="badge bg-secondary-subtle text-secondary"><%= privateArticles.length %></span>
          </h2>
          <% if (privateArticles.length > 0) { %>
            <span class="text-muted small">
              Keep iterating until you’re ready to publish.
            </span>
          <% } %>
        </div>

        <% if (privateArticles.length === 0) { %>
          <section class="app-card p-5 text-center">
            <i class="fas fa-user-secret fa-3x text-secondary mb-3"></i>
            <h3 class="fw-bold mb-3">No private drafts yet</h3>
            <p class="text-muted">
              Use private visibility to plan and iterate on topics before going public.
            </p>
            <a href="/articles/create" class="btn btn-secondary-pill btn-pill">
              <i class="fas fa-lightbulb"></i>
              Capture an idea
            </a>
          </section>
        <% } else { %>
          <section class="article-grid">
            <% privateArticles.forEach(article => { %>
              <article class="article-card h-100">
                <% const privateHero = (article.media || []).find(item => item.type === 'image') %>
                <% if (privateHero) { %>
                  <img src="<%= privateHero.url || ('/' + privateHero.storagePath) %>" class="w-100" style="height: 180px; object-fit: cover;" alt="<%= article.title || 'Article image' %>">
                <% } %>
                <div class="article-header d-flex justify-content-between align-items-start">
                  <div>
                    <h3 class="h5 mb-1"><%= article.title || 'Untitled Article' %></h3>
                    <small class="text-muted">
                      <i class="fas fa-calendar me-1"></i>
                      <%= new Date(article.createdAt).toLocaleString('en-US', {
                        year: 'numeric',
                        month: 'short',
                        day: 'numeric',
                        hour: '2-digit',
                        minute: '2-digit'
                      }) %>
                    </small>
                  </div>
                  <span class="badge-soft bg-secondary-subtle text-secondary">
                    <i class="fas fa-lock me-1"></i>
                    Private
                  </span>
                </div>
                <div class="article-body">
                  <% const privatePreview = article.body ? article.body.replace(/<[^>]+>/g, '') : '' %>
                  <p><%= privatePreview ? (privatePreview.length > 160 ? privatePreview.substring(0, 160) + '…' : privatePreview) : 'No content captured yet.' %></p>

                  <% const privateDocs = (article.media || []).filter(item => item.type === 'document') %>
                  <% const privateVideos = (article.media || []).filter(item => item.type === 'video') %>
                  <% if (privateDocs.length || privateVideos.length) { %>
                    <div class="d-flex flex-wrap gap-2 mt-3">
                      <% if (privateDocs.length) { %>
                        <span class="badge-soft bg-primary-subtle text-primary">
                          <i class="fas fa-file me-1"></i>
                          <%= privateDocs.length %> doc<%= privateDocs.length > 1 ? 's' : '' %>
                        </span>
                      <% } %>
                      <% if (privateVideos.length) { %>
                        <span class="badge-soft bg-secondary-subtle text-secondary">
                          <i class="fas fa-video me-1"></i>
                          <%= privateVideos.length %> video<%= privateVideos.length > 1 ? 's' : '' %>
                        </span>
                      <% } %>
                    </div>
                  <% } %>
                </div>
                <div class="article-actions">
                  <a href="/articles/<%= article.id %>" class="btn btn-primary-pill btn-pill btn-sm">
                    <i class="fas fa-eye"></i>
                    View
                  </a>
                  <a href="/articles/update/<%= article.id %>" class="btn btn-warning-pill btn-pill btn-sm">
                    <i class="fas fa-edit"></i>
                    Edit
                  </a>
                  <form method="POST" action="/articles/<%= article.id %>?_method=DELETE" class="d-inline">
                    <button type="submit" class="btn btn-danger-pill btn-pill btn-sm"
                      onclick="return confirm('Delete this draft? This cannot be undone.')">
                      <i class="fas fa-trash"></i>
                      Delete
                    </button>
                  </form>
                </div>
              </article>
            <% }) %>
          </section>
        <% } %>
      </section>

      <section class="mb-5">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3 mb-3">
          <h2 class="h4 fw-bold mb-0 d-flex align-items-center gap-2">
            <i class="fas fa-globe text-success"></i>
            Public Showcase
            <span class="badge bg-success-subtle text-success"><%= publicArticles.length %></span>
          </h2>
          <% if (publicArticles.length > 0) { %>
            <span class="text-muted small">
              Your published articles appear on the global feed.
            </span>
          <% } %>
        </div>

        <% if (publicArticles.length === 0) { %>
          <section class="app-card p-5 text-center">
            <i class="fas fa-bullhorn fa-3x text-success mb-3"></i>
            <h3 class="fw-bold mb-3">Nothing public yet</h3>
            <p class="text-muted">
              Publish an article to share it with the DevOps community.
            </p>
            <a href="/articles/create" class="btn btn-primary-pill btn-pill">
              <i class="fas fa-share"></i>
              Publish something new
            </a>
          </section>
        <% } else { %>
          <section class="article-grid">
            <% publicArticles.forEach(article => { %>
              <article class="article-card h-100">
                <% const publicHero = (article.media || []).find(item => item.type === 'image') %>
                <% if (publicHero) { %>
                  <img src="<%= publicHero.url || ('/' + publicHero.storagePath) %>" class="w-100" style="height: 180px; object-fit: cover;" alt="<%= article.title || 'Article image' %>">
                <% } %>
                <div class="article-header d-flex justify-content-between align-items-start">
                  <div>
                    <h3 class="h5 mb-1 text-primary"><%= article.title || 'Untitled Article' %></h3>
                    <small class="text-muted">
                      <i class="fas fa-calendar me-1"></i>
                      <%= new Date(article.createdAt).toLocaleDateString('en-US', {
                        year: 'numeric',
                        month: 'short',
                        day: 'numeric'
                      }) %>
                    </small>
                  </div>
                  <span class="badge-soft bg-success-subtle text-success">
                    <i class="fas fa-globe me-1"></i>
                    Public
                  </span>
                </div>
                <div class="article-body">
                  <% const publicPreview = article.body ? article.body.replace(/<[^>]+>/g, '') : '' %>
                  <p><%= publicPreview ? (publicPreview.length > 160 ? publicPreview.substring(0, 160) + '…' : publicPreview) : 'No content available.' %></p>

                  <% const publicDocs = (article.media || []).filter(item => item.type === 'document') %>
                  <% const publicVideos = (article.media || []).filter(item => item.type === 'video') %>
                  <% if (publicDocs.length || publicVideos.length) { %>
                    <div class="d-flex flex-wrap gap-2 mt-3">
                      <% if (publicDocs.length) { %>
                        <span class="badge-soft bg-primary-subtle text-primary">
                          <i class="fas fa-file me-1"></i>
                          <%= publicDocs.length %> doc<%= publicDocs.length > 1 ? 's' : '' %>
                        </span>
                      <% } %>
                      <% if (publicVideos.length) { %>
                        <span class="badge-soft bg-secondary-subtle text-secondary">
                          <i class="fas fa-video me-1"></i>
                          <%= publicVideos.length %> video<%= publicVideos.length > 1 ? 's' : '' %>
                        </span>
                      <% } %>
                    </div>
                  <% } %>
                </div>
                <div class="article-actions">
                  <a href="/articles/<%= article.id %>" class="btn btn-primary-pill btn-pill btn-sm">
                    <i class="fas fa-eye"></i>
                    View
                  </a>
                  <a href="/articles/update/<%= article.id %>" class="btn btn-warning-pill btn-pill btn-sm">
                    <i class="fas fa-edit"></i>
                    Edit
                  </a>
                  <form method="POST" action="/articles/<%= article.id %>?_method=DELETE" class="d-inline">
                    <button type="submit" class="btn btn-danger-pill btn-pill btn-sm"
                      onclick="return confirm('Delete this article? This cannot be undone.')">
                      <i class="fas fa-trash"></i>
                      Delete
                    </button>
                  </form>
                </div>
              </article>
            <% }) %>
          </section>
        <% } %>
      </section>
    </div>
  </main>

  <a href="/articles/create" class="fab-create" title="Create new article">
    <i class="fas fa-plus"></i>
  </a>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
