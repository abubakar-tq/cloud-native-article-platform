<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Sign In - DevOps Articles</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/app.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark app-navbar">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="/articles">
        <i class="fas fa-feather-alt me-2"></i>
        DevOps Articles
      </a>
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/auth/signup">
          <i class="fas fa-user-plus me-1"></i>
          Create account
        </a>
      </div>
    </div>
  </nav>

  <main class="app-shell">
    <div class="container">
      <div class="row g-4 align-items-center">
        <div class="col-lg-6">
          <div class="hero-banner h-100">
            <h1 class="fw-bold mb-3">
              Welcome back, creator.
            </h1>
            <p class="lead text-muted mb-0">
              Sign in to continue writing, explore the latest DevOps stories, and manage your submissions.
            </p>
          </div>
        </div>
        <div class="col-lg-6">
          <section class="form-wrapper">
            <h2 class="h4 fw-bold mb-3 text-center">Sign in to your workspace</h2>
            <p class="text-muted text-center mb-4">
              Use the account you created when you first published with us.
            </p>

            <%- include('../partials/flash') %>

            <% if (errors && errors.length) { %>
              <div class="alert alert-danger">
                <ul class="mb-0">
                  <% errors.forEach(function(error) { %>
                    <li><%= error %></li>
                  <% }) %>
                </ul>
              </div>
            <% } %>

            <form method="POST" action="/auth/login" novalidate>
              <div class="mb-3">
                <label for="email" class="form-label">Email address</label>
                <input
                  type="email"
                  class="form-control"
                  id="email"
                  name="email"
                  placeholder="you@example.com"
                  value="<%= email || '' %>"
                  required
                >
              </div>

              <div class="mb-4">
                <label for="password" class="form-label">Password</label>
                <input
                  type="password"
                  class="form-control"
                  id="password"
                  name="password"
                  required
                >
              </div>

              <div class="mb-4">
                <div class="form-check">
                  <input 
                    class="form-check-input" 
                    type="checkbox" 
                    name="keepLoggedIn" 
                    id="keepLoggedIn"
                    value="true"
                  >
                  <label class="form-check-label" for="keepLoggedIn">
                    <i class="fas fa-clock me-1 text-primary"></i>
                    Keep me logged in (7 days)
                  </label>
                </div>
              </div>

              <button type="submit" class="btn btn-primary-pill btn-pill w-100 mb-3">
                <i class="fas fa-right-to-bracket me-1"></i>
                Sign in
              </button>
              
              <div class="text-center mb-3">
                <a href="/auth/forgot-password" class="text-decoration-none">
                  <i class="fas fa-key me-1"></i>
                  Forgot your password?
                </a>
              </div>
            </form>

            <hr class="my-4">
            <div class="text-center mb-3">
              <a href="/auth/admin/login" class="btn btn-outline-warning btn-sm">
                <i class="fas fa-shield-alt me-1"></i>
                Admin Login
              </a>
            </div>
            <p class="text-center mb-0">
              Need an account?
              <a href="/auth/signup" class="text-decoration-none fw-semibold">Create one now</a>
            </p>
          </section>
        </div>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
