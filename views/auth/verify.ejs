<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Verify Account - DevOps Articles</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="/css/app.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark app-navbar">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="/articles">
        <i class="fas fa-feather-alt me-2"></i>
        DevOps Articles
      </a>
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/auth/login">
          <i class="fas fa-right-to-bracket me-1"></i>
          Sign in
        </a>
      </div>
    </div>
  </nav>

  <main class="app-shell">
    <div class="container">
      <div class="row g-4 align-items-center">
        <div class="col-lg-6">
          <div class="hero-banner h-100">
            <h1 class="fw-bold mb-3">Verify &amp; secure your account</h1>
            <p class="lead text-muted mb-0">
              We just sent a six-digit code to <strong><%= email %></strong>. Enter it below and choose a password to complete setup.
            </p>
          </div>
        </div>
        <div class="col-lg-6">
          <section class="form-wrapper">
            <h2 class="h4 fw-bold mb-3 text-center">Enter your verification code</h2>

            <%- include('../partials/flash') %>

            <% if (errors && errors.length) { %>
              <div class="alert alert-danger">
                <ul class="mb-0">
                  <% errors.forEach(function(error) { %>
                    <li><%= error %></li>
                  <% }) %>
                </ul>
              </div>
            <% } %>

            <form method="POST" action="/auth/verify" novalidate>
              <input type="hidden" name="email" value="<%= email %>">

              <div class="mb-3">
                <label for="otp" class="form-label">Verification code</label>
                <input
                  type="text"
                  inputmode="numeric"
                  pattern="[0-9]{6}"
                  maxlength="6"
                  class="form-control"
                  id="otp"
                  name="otp"
                  placeholder="6-digit code"
                  required
                >
              </div>

              <div class="mb-3">
                <label for="password" class="form-label">Create a password</label>
                <input
                  type="password"
                  class="form-control"
                  id="password"
                  name="password"
                  minlength="8"
                  placeholder="At least 8 characters"
                  required
                >
              </div>

              <div class="mb-4">
                <label for="confirmPassword" class="form-label">Confirm password</label>
                <input
                  type="password"
                  class="form-control"
                  id="confirmPassword"
                  name="confirmPassword"
                  minlength="8"
                  placeholder="Re-enter your password"
                  required
                >
              </div>

              <button type="submit" class="btn btn-primary-pill btn-pill w-100">
                <i class="fas fa-check-circle me-1"></i>
                Verify &amp; create account
              </button>
            </form>

            <hr class="my-4">
            <form method="POST" action="/auth/resend" class="d-flex justify-content-between align-items-center">
              <input type="hidden" name="email" value="<%= email %>">
              <span class="text-muted">Didn&apos;t get the code?</span>
              <button type="submit" class="btn btn-link text-decoration-none">
                <i class="fas fa-sync me-1"></i>
                Resend
              </button>
            </form>

            <p class="text-center mt-3 mb-0">
              Already verified?
              <a href="/auth/login" class="text-decoration-none fw-semibold">Sign in here</a>
            </p>
          </section>
        </div>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
